fix(phase-b): Track C self-review — surface malformed env + drop dead-code silencer

Self-review of PR #135 (Track C — B-6 Tenant API hardening) with
production-code-flip verification on the four key invariants. One real
B-level finding fixed (silent malformed env masks operator typos) +
one C-level dead-code drop. CRLF→LF normalisation on group.go +
task.go from python-driven flip tests during invariant verification
(no semantic change; vet + tests green).

A-level (verified non-vacuous via production-code-flip):

  * RateLimit Retry-After header — drop the header set, test FAILs
    "Retry-After header missing" → invariant rooted in production
  * PutGroup forbidden-member check — comment out the if-guard,
    AllMembersForbidden_ListsAllInError FAILs (request proceeds to
    git commit which ungated git missing → 500 not 403) →
    confirms the check IS the gate, not collateral
  * GetTask result filter — bypass the filter, NoAccessibleResults_
    Returns403 FAILs (returns 200 with full results visible) →
    confirms filter actually scrubs forbidden tenants
  * ListPRs filter — same pattern, skipped flip (low marginal value)

B-level fixes applied:

  * **B6 — silent malformed env fallback**: doc §1.2 said
    "TA_RATE_LIMIT_PER_MIN=garbage → fallback to 100 + log warning"
    but caller (cmd/server/main.go) didn't actually log. Operator
    typo'd env var would silently get default 100 with NO indication
    of the typo. Fix:
    - RateLimitConfigFromEnv signature (RateLimitConfig) →
      (RateLimitConfig, malformed bool)
    - main.go logs explicit "WARN: TA_RATE_LIMIT_PER_MIN=... is
      malformed" when malformed=true
    - 5 existing tests updated to check the new bool; "empty"/
      "valid integer"/"0" all explicitly assert malformed=false
      (not just default fallback) so future refactor that
      accidentally widens the malformed surface gets caught
    - doc §1.2 zh+en updated to reflect new behaviour

B-level OK (no fix needed, verified):

  * Middleware ordering: RequestID → RequestIDResponse → RealIP
    → Logger → ... → RateLimit. RealIP middleware corrects
    r.RemoteAddr from X-Forwarded-For; my limiter's caller
    fallback chain reads X-Real-IP first then RemoteAddr, both
    work correctly in production (oauth2-proxy sets X-Forwarded-
    For or X-Real-IP depending on deployment) and in test (httptest
    populates RemoteAddr).
  * Batch endpoints (POST /api/v1/tenants/batch + /api/v1/groups/
    {id}/batch) already enforce per-tenant PermWrite inside the
    handler since v2.6.0 / v2.5.0 — confirmed not a gap.
  * ListTenants RBAC-filtered since v2.5.0 (filterTenantsByRBAC)
    — already protected, doc §3.1 correctly omits it from "newly
    hardened" list.
  * Open-mode write-deny: rbac.HasPermission L145-148 returns
    "want == PermRead" only when cfg.Groups is empty — doc §3.5
    claim of "PermWrite denied in open mode" is accurate.

C-level cleanup:

  * authz_test.go had `var _ = chi.URLParam` silencer + chi import
    that nothing else used. Same dead-code pattern Track B B1
    review caught (`nestedTenantHash` helper). Removed both —
    dev-rules anti-dead-code principle. Tests still green; chi
    is used in handler_test.go via newRequestWithChiParam helper
    (same package, no import needed in this file).

Test sweep post-fix: 14/14 authz_test cases + 15/15 middleware_test
cases (5 ConfigFromEnv cases now also assert the malformed bool —
"empty"/"valid"/"zero" all explicitly assert malformed=false) + all
existing handler tests stay green. go vet clean.
