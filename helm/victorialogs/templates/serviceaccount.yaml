apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "victorialogs.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "victorialogs.labels" . | nindent 4 }}
# VictoriaLogs never calls the Kubernetes API — drop the token.
automountServiceAccountToken: false
