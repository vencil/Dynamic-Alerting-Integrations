apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "chargeback.name" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "chargeback.labels" . | nindent 4 }}
# Aggregator never calls the Kubernetes API — pure HTTP-to-VictoriaLogs.
automountServiceAccountToken: false
