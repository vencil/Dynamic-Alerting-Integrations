# Placeholder — actual tenant YAMLs are generated by:
#   python3 ../../../../scripts/tools/dx/generate_tenant_fixture.py \
#       --layout hierarchical --count 1000 --with-defaults \
#       --output ./ --seed 42
# (run from this directory, or use --output with an absolute path)
#
# Without generation, this dir contains only this _defaults.yaml so
# the threshold-exporter scanner sees a hierarchical-mode root but
# no actual tenants. Useful for exporter-only smoke tests.

defaults:
  bench_trigger: 100
