{
  "_comment": "Cross-language STATELESS per-recipe validation contract (ADR-024 §S5). The Python compiler (shape.py recipe_id + parse_threshold) and the Go exporter/preflight (resolveOneCustomAlert) MUST AGREE on accept/reject for each spec — this closes the validation-decision drift that the slug golden vectors (recipe_id_vectors.json) did NOT cover. Scope = validations BOTH perform: recipe/metric/op/horizon/selector-reserved/severity/for enums + adversarial selector-VALUE acceptance (key-valid value accepted both sides; Python _escape_value is the injection boundary at emit, not the validator). NOT here: Go-stricter preflight extras (NaN/Inf threshold, mode enum, ratio-floor) — tested per-language; and STATEFUL checks (name/severity uniqueness, cap, cross-tree) — also per-language mock-tree tests.",
  "cases": [
    {"_note": "valid threshold", "valid": true,
     "spec": {"recipe": "threshold", "name": "ok", "metric": "queue_depth", "op": ">", "window": "5m", "threshold": "100:warning"}},
    {"_note": "valid forecast (ratio mode, in-range floor)", "valid": true,
     "spec": {"recipe": "forecast", "name": "disk", "metric": "avail", "capacity_metric": "cap", "op": "<", "horizon": "4h", "threshold": "0.15:warning"}},
    {"_note": "adversarial selector VALUE accepted (key valid; value escaped at emit by Python _escape_value, never breaks the quoted literal)", "valid": true,
     "spec": {"recipe": "rate", "name": "r", "metric": "http_requests_total", "selectors_re": {"status": "5.."}, "selectors": {"path": "a\"b\\c"}, "op": ">", "window": "5m", "threshold": "10:warning"}},
    {"_note": "bad metric (colon → recording-rule reference)", "valid": false,
     "spec": {"recipe": "threshold", "name": "x", "metric": "tenant:alert:x", "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "unknown recipe", "valid": false,
     "spec": {"recipe": "bogus", "name": "x", "metric": "m", "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "bad op", "valid": false,
     "spec": {"recipe": "threshold", "name": "x", "metric": "m", "op": "!=", "window": "5m", "threshold": "1:warning"}},
    {"_note": "op == on threshold accepted (#810: status/error-code-as-value match on a raw gauge)", "valid": true,
     "spec": {"recipe": "threshold", "name": "errno_match", "metric": "mysql_semisync_master_last_errno", "op": "==", "window": "5m", "threshold": "1236:critical"}},
    {"_note": "op == rejected on a computed-value recipe (float-fragile; threshold-recipe-only gate)", "valid": false,
     "spec": {"recipe": "rate", "name": "bad_eq", "metric": "http_requests_total", "op": "==", "window": "5m", "threshold": "5:warning"}},
    {"_note": "op == rejected on ratio (otherwise-valid spec; the gate is the sole rejection reason)", "valid": false,
     "spec": {"recipe": "ratio", "name": "bad_eq_ratio", "metric": "http_errors_total", "denominator_metric": "http_requests_total", "op": "==", "window": "5m", "threshold": "0.5:warning"}},
    {"_note": "op == rejected on p99_latency (otherwise-valid spec)", "valid": false,
     "spec": {"recipe": "p99_latency", "name": "bad_eq_p99", "metric": "http_request_duration_seconds", "quantile": "0.99", "op": "==", "window": "5m", "threshold": "2:warning"}},
    {"_note": "op == rejected on forecast raw mode (otherwise-valid spec; gate fires before horizon shaping)", "valid": false,
     "spec": {"recipe": "forecast", "name": "bad_eq_fc", "metric": "queue_depth", "op": "==", "horizon": "12h", "threshold": "100:warning"}},
    {"_note": "op == rejected on absence too (#819 adversarial: op is meaningless for a presence check; the gate now rejects it in lockstep with the schema if/then editor-guard, so no front/back brain-split on API/GitOps-submitted specs)", "valid": false,
     "spec": {"recipe": "absence", "name": "bad_eq_absence", "metric": "up", "op": "==", "window": "5m", "threshold": "1:critical"}},
    {"_note": "forecast bad horizon (not enum)", "valid": false,
     "spec": {"recipe": "forecast", "name": "x", "metric": "m", "op": "<", "horizon": "3h", "threshold": "0.5:warning"}},
    {"_note": "reserved selector label", "valid": false,
     "spec": {"recipe": "rate", "name": "x", "metric": "m", "selectors": {"tenant": "foo"}, "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "bad severity", "valid": false,
     "spec": {"recipe": "threshold", "name": "x", "metric": "m", "op": ">", "window": "5m", "threshold": "1:bogus"}},
    {"_note": "bad for (not enum)", "valid": false,
     "spec": {"recipe": "threshold", "name": "x", "metric": "m", "op": ">", "window": "5m", "threshold": "1:warning", "for": "2m"}},
    {"_note": "F2: selector VALUE carrying a Go-template action rejected — it reaches the alert-annotation template context (recipes.py _alert_rule) where Prometheus evaluates {{ query ... }} across the whole TSDB; _escape_value defends only the PromQL string-literal, not the template. PortSwigger logic-less principle: untrusted data must never become template code.", "valid": false,
     "spec": {"recipe": "absence", "name": "tmpl_inj", "metric": "up", "selectors": {"probe": "{{ query `sum(user_threshold)` | first | value }}"}, "window": "5m", "threshold": "1:warning"}},
    {"_note": "F2: selector VALUE with a backtick (Go raw-string delimiter; survives _escape_value which only escapes \\ \" \\n) rejected", "valid": false,
     "spec": {"recipe": "rate", "name": "tick_inj", "metric": "http_requests_total", "selectors": {"path": "a`b"}, "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "F1: window with a PromQL injection rejected — window is raw-interpolated into rate(m[<window>]) (recipes.py); must match the Go-duration charset ^([0-9]+(ns|us|µs|ms|s|m|h))+$ (the schema pattern, now enforced imperatively both sides)", "valid": false,
     "spec": {"recipe": "rate", "name": "win_inj", "metric": "m", "op": ">", "window": "5m]) or vector(1", "threshold": "1:warning"}},
    {"_note": "F1: quantile with a PromQL injection rejected — quantile is raw-interpolated into histogram_quantile(<quantile>, ...) (recipes.py); must be a number in (0,1)", "valid": false,
     "spec": {"recipe": "p99_latency", "name": "q_inj", "metric": "http_request_duration_seconds", "quantile": "0.5) or (1", "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "F1-lockstep (Phase C hunter): quantile as a Go hex-float literal. Go strconv.ParseFloat accepts \"0x1p-1\"=0.5 but CPython float() rejects it → without a shared decimal-only regex the Go preflight goes GREEN while the Python CI compiler goes RED, landing a poison commit that wedges the platform-wide #741 S3b drift gate. Must be REJECTED by BOTH (decimal-float charset).", "valid": false,
     "spec": {"recipe": "p99_latency", "name": "qhex", "metric": "http_request_duration_seconds", "quantile": "0x1p-1", "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "#1017: dotless-exponent quantile TEXT \"95e-2\" (=0.95, in range) must be ACCEPTED by both — the parity fix pinned quantile to schema type:string (quoted) but did NOT narrow the shared decimal accept-set; exponent forms remain legal as text.", "valid": true,
     "spec": {"recipe": "p99_latency", "name": "q_exp", "metric": "http_request_duration_seconds", "quantile": "95e-2", "op": ">", "window": "5m", "threshold": "1:warning"}},
    {"_note": "slo_burn_rate valid (ADR-031): objective replaces threshold; no window (burn windows are recipe semantics); denominator_metric required; severity fixed by the recipe (no threshold tail to parse).", "valid": true,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9"}},
    {"_note": "slo_burn_rate objective=100 REJECTED — open interval (0,100): objective 100 makes the error budget 0 → threshold 0 → always fires.", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "100"}},
    {"_note": "slo_burn_rate objective=0 REJECTED — open interval (0,100): objective 0 can never fire.", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "0"}},
    {"_note": "slo_burn_rate objective=\"disable\" ACCEPTED — the tri-state opt-out, consistent with the existing threshold disable convention: the declaration still compiles; the exporter simply never emits its user_threshold series (data-plane opt-out).", "valid": true,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "disable"}},
    {"_note": "slo_burn_rate slo_period non-enum REJECTED (only 28d|30d; the period only scales the exporter-derived multipliers).", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9", "slo_period": "7d"}},
    {"_note": "slo_burn_rate min_events=0 REJECTED — must be a positive integer (0 would disable the low-traffic guard; opt out via objective:\"disable\" instead).", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9", "min_events": 0}},
    {"_note": "slo_burn_rate threshold AND objective together REJECTED — objective REPLACES threshold (severity is fixed by the recipe, fast→critical / slow→warning; a value:severity tail has no meaning).", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9", "threshold": "0.01:critical"}},
    {"_note": "slo_burn_rate missing objective REJECTED (required — there is no threshold to fall back to).", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total"}},
    {"_note": "slo_burn_rate missing denominator_metric REJECTED (the SLI is a ratio; metric alone is only the bad-events counter).", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "objective": "99.9"}},
    {"_note": "slo_burn_rate explicit op REJECTED — the recipe fixes '>' (burn rate exceeding its budget threshold); an authored op is a semantic error, not a knob.", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9", "op": "<"}},
    {"_note": "slo_burn_rate objective as a JSON NUMBER rejected — STRING-ONLY on both sides (the #1017 quantile lesson): Python rejects by isinstance(str), Go by the taggedScalar !!float tag; a bare number's text is YAML-dialect-ambiguous and a str()/re-canonicalise coercion would silently diverge the lockstep.", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": 99.9}},
    {"_note": "slo_burn_rate `for` outside the enum rejected on BOTH sides — Python via _normalize_for inside recipe_id; Go must enforce it on the slo resolve path too (resolveSloBurnRate bypasses the non-slo path's customAlertForValid check, the drift this case pins).", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9", "for": "2h"}},
    {"_note": "slo_burn_rate min_events over the 1000000 maximum rejected — an absurd floor silently disables the alert while looking configured; mirrors the schema `maximum` on both sides.", "valid": false,
     "spec": {"recipe": "slo_burn_rate", "name": "avail", "metric": "http_requests_errors_total", "denominator_metric": "http_requests_total", "objective": "99.9", "min_events": 1000001}}
  ]
}
