# ============================================================
# Platform Recording Rules — 可直接合併至 Prometheus ConfigMap
# ============================================================
# 收斂率: 3 條規則 → 0 條 Recording Rules (壓縮 100.0%)
# ============================================================

groups:
  - name: custom_migrated-recording-rules
    rules:
