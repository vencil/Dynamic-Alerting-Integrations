{
  "$comment": "路徑觸發指引的 SSOT（agent 指引改善計畫 PR-D，#1737 線）。每一列：paths（glob，對 repo 相對路徑比對）→ read（先讀哪份文件的哪一節）＋ note（一句約束）。Claude Code 由 scripts/session-guards/paths_map.py 在 Edit/Write/Bash 前注入；Cursor（.cursor/skills/vibe-paths-*/SKILL.md 的 paths:）與 Copilot（.github/instructions/vibe-paths-*.instructions.md 的 applyTo）由 gen_agent_adapters.py 投影，三家共用這一份。JSON 而非 YAML：hook 跑在 host 直譯器、產生器跑在 pre-commit venv，兩處都沒有 pyyaml。note 是會被注入到 agent 上下文的宣稱，寫之前對照 Makefile／.pre-commit-config.yaml／被點名的測試核對；read 的檔與章節有測試釘住，note 沒有。",
  "version": 1,
  "entries": [
    {
      "id": "helm-chart",
      "paths": ["helm/**"],
      "read": [
        {"file": "docs/internal/windows-mcp-playbook.md", "section": "Helm Upgrade 防衝突"},
        {"file": "docs/internal/iac-lint-baseline.md", "section": "Layer 2 — Helm template"}
      ],
      "note": "Helm SAST L2 只在 CI 硬擋，本地要手動跑 `pre-commit run --hook-stage manual --all-files iac-helm-sast-check`；values 裡的 secret-shape 是 L3，同一份 baseline 列管。新增 value→env 接線後跑 `helm template` 並 grep 那個 env：`{{- with X }}` 區塊內 `.` 被重綁成 X——裡面用 `.foo` 去拿 root 的 key 會靜默輸出空字串、X 的路徑拼錯則整段靜默不輸出，兩種 `helm template` 都 rc=0（寫 `.Values.foo` 反而會報錯）；要 root 用 `$.Values.foo`。改 portal 部署精靈會帶的 image（`generate_platform_data.py` 的 `WIZARD_THIRD_PARTY`／`WIZARD_FIRST_PARTY`）的 tag，是同一顆 commit 裡的三件事：`make platform-data` 重生 `docs/assets/platform-data.json`、同步手抄鏡像 `tools/portal/src/interactive/tools/_common/data/images.js`（`images-fallback-drift.test.ts` 逐 key 比對）、`make portal-build` 重建 dist。"
    },
    {
      "id": "k8s-manifests",
      "paths": ["k8s/**", "operator-manifests/**"],
      "read": [
        {"file": "docs/internal/iac-lint-baseline.md", "section": "Layer 4 — k8s raw manifest"}
      ],
      "note": "raw manifest 的 SAST L4 是 manual hook：`pre-commit run --hook-stage manual --all-files k8s-manifests-sast-check`；`k8s/03-monitoring/configmap-rules-*.yaml` 是 rule-packs 的產物，改來源再 `make rulepack-configmaps`。改精靈會帶的 image（`WIZARD_THIRD_PARTY`／`WIZARD_FIRST_PARTY`）的 tag，同 helm-chart 那三件事（platform-data → `images.js` 鏡像 → dist）；其他 image 不牽動 portal。"
    },
    {
      "id": "rule-packs-confd",
      "paths": ["rule-packs/**", "components/threshold-exporter/config/conf.d/**", "try-local/seed/conf.d/**"],
      "read": [
        {"file": "docs/internal/testing-playbook.md", "section": "conf.d/ YAML 格式陷阱"},
        {"file": "docs/internal/testing-playbook.md", "section": "PromQL rule-test traps"}
      ],
      "note": "conf.d 是 wrapped format（`tenants:` 包一層）；rule-pack 改動後的產物各有各的重生指令與 drift hook——`python3 scripts/tools/dx/generate_rule_pack_stats.py --generate --lang all`（`rule-pack-stats-check`）、`make rulepack-configmaps`（副本 drift 由 `rulepack-configmap-drift`／`rulepack-3copy-drift` 擋）、`make platform-data`（`platform-data-check`）——一支跑完不代表另幾道會綠；Go／PromQL／fixture 不得 hardcode tenant id。"
    },
    {
      "id": "go-code",
      "paths": ["**/*.go"],
      "read": [
        {"file": "docs/internal/test-map.md", "section": "Go tests（Dev Container"},
        {"file": "docs/internal/test-map.md", "section": "測試注入 Seam"}
      ],
      "note": "Go 測試在 Dev Container（`make dc-go-test MOD=… PKG=…`），且 `dc-*` 恆定作用於主 worktree 掛載——在 worktree 改完直接跑會測到舊 code（Trap #62）；`go vet` 不是 CI 的 linter，推前在容器裡對改到的每個 go module 跑一次 `golangci-lint run ./...`。"
    },
    {
      "id": "tenant-api-go",
      "paths": ["components/tenant-api/**/*.go"],
      "read": [
        {"file": "docs/internal/hook-vs-skill-coverage.md", "section": "4.5 ⚙️ CI-only gates"}
      ],
      "note": "OpenAPI spec 會因為任何 swag 標註可達的 struct 改動而漂移，不只 handler 標註：改完跑 `make api-docs`，再 `git diff --exit-code components/tenant-api/docs/`。CI 上它紅在 `go-tests-tenant-api` job 的「Verify OpenAPI spec is up-to-date」步驟，看起來像 Go 測試失敗。"
    },
    {
      "id": "portal",
      "paths": ["tools/portal/**", "docs/assets/dist/**"],
      "read": [
        {"file": "tools/portal/CLAUDE.md"}, {"file": "docs/internal/jsx-multi-file-pattern.md", "section": "Build & verify loop"}
      ],
      "note": "改 source 後自己跑 `make portal-build` 重生提交的 dist——「改了 source 忘了重 build」沒有任何 hook 擋（`dist-source-consistency-check` 只抓反向：提交了 dist 卻沒有 source 變更）；`make test-portal` 跑 Vitest；Windows host 要在 worktree 內 build。"
    },
    {
      "id": "playwright-e2e",
      "paths": ["tests/e2e/**"],
      "read": [
        {"file": "docs/internal/testing-playbook.md", "section": "Playwright E2E 測試"}
      ],
      "note": "`playwright-lint` pre-commit 需要 `tests/e2e/node_modules`（web session 由 session-start hook 裝）；spec 內的 `test.fixme()` 有治理規則，別拿它當跳過。`@visual` 的 baseline（`tests/e2e/*-snapshots/*.png`）走 Git LFS 且與環境相依，重生用 `.github/workflows/visual-baseline.yaml` 的 workflow_dispatch，不在本機產。"
    },
    {
      "id": "python-tools",
      "paths": ["scripts/tools/**/*.py"],
      "read": [
        {"file": "docs/internal/testing-playbook.md", "section": "SAST 合規"},
        {"file": "docs/internal/dev-rules.md", "section": "13. da-tools 子命令 exit-code"},
        {"file": "docs/internal/lint-policy.md", "section": "7. 新增 lint 的審核 checklist"}
      ],
      "note": "新工具要守 exit code 0/1/2 與 `--ci`／`--json` 約定，`subprocess` 帶 timeout、`open` 帶 encoding；docstring 改了跑 `generate_tool_map.py --generate --lang all`；改到 `tests/shared/property-coverage.yaml` 已宣告的那幾個模組時，新 module-level 函式要進它的 triage（其他模組不受該 lint 管）。"
    },
    {
      "id": "python-tests",
      "paths": ["tests/**/*.py"],
      "read": [{"file": "docs/internal/test-map.md", "section": "測試基礎設施"}],
      "note": "掃 repo 檔用 `tests/_tree.py` 的 `repo_files()`，不要 `REPO_ROOT.rglob`（會把 `.claude/worktrees` 裡的 repo 副本當成本樹）；本地驗證新 CLI 工具要跑 `pytest tests/`，管它的契約測試在 `tests/shared/`、`tests/ops/`，不在工具自己那層。"
    },
    {
      "id": "changelog",
      "paths": ["CHANGELOG.md"],
      "read": [
        {"file": "docs/internal/commit-convention.md", "section": "Editing `CHANGELOG.md` by hand"},
        {"file": "docs/internal/hook-vs-skill-coverage.md", "section": "3. Pre-commit auto hooks"}
      ],
      "note": "只動 `## [Unreleased]`；整段對 base 每新 bullet ≤ 1,000 字元，量測數字放 commit／issue 不放這裡：`python3 scripts/tools/dx/generate_changelog.py --base origin/main --lint CHANGELOG.md`。rebase／解衝突碰到這個檔，之後跑 `python3 scripts/tools/dx/changelog_rebase_check.py`：條目被靜默吃掉或重複時，數量常常不變。"
    },
    {
      "id": "agents-ssot",
      "paths": [".agents/**"],
      "read": [
        {"file": "AGENTS.md"}
      ],
      "note": "改完跑 `python3 scripts/tools/dx/gen_agent_adapters.py --generate`；`.claude/skills`、`.claude/agents`、`.cursor/skills/vibe-paths-*`、`.github/instructions/vibe-paths-*` 全是它的產物，`gen-agent-adapters-check` 擋漂移。"
    },
    {
      "id": "generated-adapters",
      "paths": [".claude/skills/**", ".claude/agents/**", ".cursor/skills/vibe-paths-*/**", ".github/instructions/vibe-paths-*"],
      "read": [
        {"file": "AGENTS.md"}
      ],
      "note": "⛔ 這是產生物：改 `.agents/` 的來源再重生；直接改這裡會被 `gen-agent-adapters-check` 擋下，下次 `--generate` 也會覆蓋（同目錄裡非 `vibe-paths-` 前綴的檔不是產物，產生器不碰）。"
    },
    {
      "id": "docs",
      "paths": ["docs/**/*.md"],
      "read": [
        {"file": "docs/internal/dev-rules.md", "section": "10. 雙語政策"},
        {"file": "docs/internal/dev-rules.md", "section": "4. Doc-as-Code"},
        {"file": "docs/internal/doc-template.md", "section": "5. 自動化檢查"}
      ],
      "note": "`python3 scripts/tools/lint/check_doc_links.py --ci` 驗連結與錨點；`docs/internal/**` 不需 `.en.md`，其餘外部面向文件雙語同步；push 時 mkdocs strict 對被推的 commit 建站。"
    },
    {
      "id": "workflows",
      "paths": [".github/workflows/**"],
      "read": [
        {"file": "docs/internal/agent-rulebook-shapes.md", "section": "D-04 有 gate ≠ gate 會跑"},
        {"file": "tests/ops/test_ci_path_filter_coverage.py"}
      ],
      "note": "path-gated job 裡的 step 不得帶計算式 `if:`（條件寫進 shell）；pytest 讀到的 repo 檔要在 `python` filter 內（且進 `PYTHON_ENTRIES_THIS_SCANNER_JUSTIFIES`）——`tests/ops/test_ci_path_filter_coverage.py` 是那道守衛，其 docstring 列了它拒收與看不見的形狀；fixture 檔名別用真實 repo 檔名。"
    },
    {
      "id": "session-guards",
      "paths": ["scripts/session-guards/**", ".claude/settings.json"],
      "read": [
        {"file": "docs/internal/hook-vs-skill-coverage.md", "section": "2. PreToolUse session-guards"}
      ],
      "note": "hook 命令一律經 `run-hooks.sh`（裸 `python` 在 Windows 是 Store stub，#824）；新 hook 的 AC 必含 live-fire 證據；`check_session_guard_liveness.py --ci` 在 commit 時驗接線與直譯器。"
    }
  ]
}
