{
  "$comment": "路徑觸發指引的 SSOT（agent 指引改善計畫 PR-D）。每一列：paths（glob，對 repo 相對路徑比對）→ read（先讀哪份文件的哪一節）＋ note（一句約束）。Claude Code 由 scripts/session-guards/paths_map.py 在 Edit/Write/Bash 前注入；Cursor（.cursor/skills/vibe-paths-*/SKILL.md 的 paths:）與 Copilot（.github/instructions/*.instructions.md 的 applyTo）由 gen_agent_adapters.py 投影，三家共用這一份。JSON 而非 YAML：hook 跑在 host 直譯器、產生器跑在 pre-commit venv，兩處都沒有 pyyaml。",
  "version": 1,
  "entries": [
    {
      "id": "helm-chart",
      "paths": ["helm/**"],
      "read": [
        {"file": "docs/internal/windows-mcp-playbook.md", "section": "Helm Upgrade 防衝突"},
        {"file": "docs/internal/iac-lint-baseline.md", "section": "Layer 2 — Helm template"}
      ],
      "note": "Helm SAST L2 只在 CI 硬擋，本地要手動跑 `pre-commit run --hook-stage manual --all-files iac-helm-sast-check`；values 裡的 secret-shape 是 L3，同一份 baseline 列管。"
    },
    {
      "id": "k8s-manifests",
      "paths": ["k8s/**", "operator-manifests/**"],
      "read": [
        {"file": "docs/internal/iac-lint-baseline.md", "section": "Layer 4 — k8s raw manifest"}
      ],
      "note": "raw manifest 的 SAST L4 是 manual hook：`pre-commit run --hook-stage manual --all-files k8s-manifests-sast-check`；`k8s/03-monitoring/configmap-rules-*.yaml` 是 rule-packs 的產物，改來源再重生。"
    },
    {
      "id": "rule-packs-confd",
      "paths": ["rule-packs/**", "components/threshold-exporter/config/conf.d/**", "try-local/seed/conf.d/**"],
      "read": [
        {"file": "docs/internal/testing-playbook.md", "section": "conf.d/ YAML 格式陷阱"}
      ],
      "note": "conf.d 是 wrapped format（`tenants:` 包一層）；rule-pack 改動後 `make platform-data` 重生數據，`rule-pack-stats-check` 與 `platform-data-check` 會擋 drift；Go／PromQL／fixture 不得 hardcode tenant id。"
    },
    {
      "id": "go-code",
      "paths": ["components/**/*.go"],
      "read": [
        {"file": "docs/internal/test-map.md", "section": "測試注入 Seam"}
      ],
      "note": "Go 測試在 Dev Container（`make dc-go-test MOD=… PKG=…`）；`go vet` 不是 CI 的 linter——推前在容器裡跑 `golangci-lint run ./...`（tenant-api 與 threshold-exporter/app 各一次）。"
    },
    {
      "id": "portal",
      "paths": ["tools/portal/**", "docs/assets/dist/**"],
      "read": [
        {"file": "docs/internal/jsx-multi-file-pattern.md", "section": "Build & verify loop"}
      ],
      "note": "`make portal-build` 後 dist 必須與 source 一致（`dist-source-consistency-check`）；`make test-portal` 跑 Vitest；Windows host 要在 worktree 內 build，別讓 main checkout 的 dist 混進來。"
    },
    {
      "id": "playwright-e2e",
      "paths": ["tests/e2e/**"],
      "read": [
        {"file": "docs/internal/testing-playbook.md", "section": "Playwright E2E 測試"}
      ],
      "note": "`playwright-lint` pre-commit 需要 `tests/e2e/node_modules`（web session 由 session-start hook 裝）；spec 內的 `test.fixme()` 有治理規則，別拿它當跳過。"
    },
    {
      "id": "python-tools",
      "paths": ["scripts/tools/**/*.py"],
      "read": [
        {"file": "docs/internal/testing-playbook.md", "section": "SAST 合規"},
        {"file": "docs/internal/dev-rules.md", "section": "13. da-tools 子命令 exit-code"}
      ],
      "note": "新工具要守 exit code 0/1/2 與 `--ci`／`--json` 約定，`subprocess` 帶 timeout、`open` 帶 encoding；docstring 改了跑 `generate_tool_map.py --generate --lang all`；新 module-level 函式要進 `tests/shared/property-coverage.yaml` triage。"
    },
    {
      "id": "changelog",
      "paths": ["CHANGELOG.md"],
      "read": [
        {"file": "docs/internal/hook-vs-skill-coverage.md", "section": "3. Pre-commit auto hooks"}
      ],
      "note": "只動 `## [Unreleased]`；整段對 base 每新 bullet ≤ 1,000 字元，量測數字放 commit／issue 不放這裡：`python3 scripts/tools/dx/generate_changelog.py --base origin/main --lint CHANGELOG.md`。"
    },
    {
      "id": "agents-ssot",
      "paths": [".agents/**"],
      "read": [
        {"file": "AGENTS.md"}
      ],
      "note": "改完跑 `python3 scripts/tools/dx/gen_agent_adapters.py --generate`；`.claude/skills`、`.claude/agents`、`.cursor/skills`、`.github/instructions` 全是它的產物，`gen-agent-adapters-check` 擋漂移。"
    },
    {
      "id": "generated-adapters",
      "paths": [".claude/skills/**", ".claude/agents/**", ".cursor/skills/**", ".github/instructions/**"],
      "read": [
        {"file": "AGENTS.md"}
      ],
      "note": "⛔ 這是產生物：改 `.agents/` 的來源再重生；直接改這裡會被 `gen-agent-adapters-check` 擋下，下次 `--generate` 也會覆蓋。"
    },
    {
      "id": "docs",
      "paths": ["docs/**/*.md"],
      "read": [
        {"file": "docs/internal/dev-rules.md", "section": "10. 雙語政策"},
        {"file": "docs/internal/dev-rules.md", "section": "4. Doc-as-Code"}
      ],
      "note": "`python3 scripts/tools/lint/check_doc_links.py --ci` 驗連結與錨點；`docs/internal/**` 不需 `.en.md`，其餘外部面向文件雙語同步；push 時 mkdocs strict 對被推的 commit 建站。"
    },
    {
      "id": "workflows",
      "paths": [".github/workflows/**"],
      "read": [
        {"file": "docs/internal/hook-vs-skill-coverage.md", "section": "4.5 ⚙️ CI-only gates"}
      ],
      "note": "path-gated job 裡的 step 不得帶計算式 `if:`（條件寫進 shell）；pytest 讀到的 repo 檔要在 `python` filter 內——`tests/ops/test_ci_path_filter_coverage.py` 是那道守衛，且 fixture 檔名別用真實 repo 檔名。"
    },
    {
      "id": "session-guards",
      "paths": ["scripts/session-guards/**", ".claude/settings.json"],
      "read": [
        {"file": "docs/internal/hook-vs-skill-coverage.md", "section": "2. PreToolUse session-guards"}
      ],
      "note": "hook 命令一律經 `run-hooks.sh`（裸 `python` 在 Windows 是 Store stub，#824）；新 hook 的 AC 必含 live-fire 證據；`check_session_guard_liveness.py --ci` 在 commit 時驗接線與直譯器。"
    }
  ]
}
