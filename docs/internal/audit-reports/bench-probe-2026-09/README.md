# 機制 1（寫入路徑 writeback flush）的量測（2026-09-04）

> 主要消費者是 [#1497](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1497) 驗收條件第 1 項（「有一個**量測**回答缺陷 vs 特性」）裡唯一還沒被碰過的候選成因。
> 隔壁 [`bench-aa-2026-08/`](../bench-aa-2026-08/README.md) 是同一條線的前一份（A/A 校準與迭代層級成本）。

## 一句話

**機制 1 存在、抓到了，而且比需要的小一個數量級以上。** 三次同日 dispatch、90 個量測輪，寫入路徑確實有 writeback 尾巴（單次寫入最慢 6.0–8.9 ms，其中一次 dispatch 的 `write_p99` 是另兩次的 **20.9×** 與 **24.6×**）；但要產生 #1497 那個 26.40 pp 的量級，寫入路徑得多出 **976 ms**，而 90 輪裡最大的一次寫入路徑超額是 **50.3 ms**——**差 19 倍**。

⛔ **這不等於回答了 #1497 的「缺陷 vs 特性」，也不等於打死機制 1。** 上面那個 19× 是對**已觀測** episode 的上界；本次三次 dispatch **沒有重現**票裡的雙峰（三個中位數的散佈是 6.75%，票裡的高低模差距是 26.40 pp），所以高模式本身沒有被抓到。

⭐ **順帶量到一件本來不在問題清單上的事**：這 90 輪之間的變異**不是 episode，是水位平移**——慢的那一輪連 p50 都變慢，而尾巴沒動。詳下方「三、這批變異是什麼形狀」。這對機制 1 是**反向**訊號，因為機制 1 預測的正是 episode。

## 受測物識別特徵

⛔ 這條線反覆燒過「數字跑在錯的樹上」，所以識別特徵在任何比較之前先印，而且由執行中的程式自己印（`PROBEENV`，來自 `runtime.Version()`），不靠人從 shell 抄。

| | |
|---|---|
| workflow | `.github/workflows/bench-probe-write-latency.yaml`，`rounds=30` `iters=400` |
| runs | [33881597190](https://github.com/vencil/Dynamic-Alerting-Integrations/actions/runs/33881597190) / [33881616306](https://github.com/vencil/Dynamic-Alerting-Integrations/actions/runs/33881616306) / [33881623584](https://github.com/vencil/Dynamic-Alerting-Integrations/actions/runs/33881623584) |
| head | `e42526e57d90a08f0537d7d45637ef18219f96fc`（三次相同） |
| runner | AMD EPYC 7763 64-Core、`nproc=4`（**三次相同**，由 job log 的 `model name` 逐次核對） |
| toolchain | `go1.26.7`，PATH 與模組內同版（`go_on_path` / `go_in_module` 兩行都印了） |
| `PROBEENV` | `goos=linux goarch=amd64 numcpu=4 go=go1.26.7 iters=400`，三次去重後**只有一組** |

⇒ 三次 dispatch 的中位輪相差 6.75%，而它們**同一個 host class**——那個差不是 CPU 型號造成的。

⚠️ **一個沒有對齊的量級，明寫**：探針每迭代 3698/400 = **9.25 ms**。這條線自己的 CI 量測有兩族：#1497 的 M/W 二分是 8.69 → 12.12 ms、[#1572](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1572) 的 benchstat 是 17.44 → 19.83 ms。9.25 ms 落在第一族的量級，但比該族 post-#1521 那一點低 24%（那一點量在 9V74 上、今天量在 7763 上）；與第二族仍差約 2×。**這一項本輪沒有去對齊**，它是交接件 §2 已經記著的未結項，不是本輪新開的。

## 重算

```bash
python3 scripts/tools/dx/analyze_probe.py --archive docs/internal/audit-reports/bench-probe-2026-09
```

不連網、不重跑 benchmark、不需要 Go。它回答的是「本文的數字能不能從收進來的資料重算出來」，**不是**「今天重跑會得到什麼」。

⛔ **這支腳本原本就放在本目錄裡，已於 TRK-373（[#1731](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1731)）搬到 `scripts/tools/dx/`。** 搬家的理由不是整理：它與 `bench-probe-write-latency.yaml` 的 Summarize step 曾是**同一份計算的兩份拷貝**，沒有任何機制保證兩者一致，而實測已累積**六處**行為分歧（五處在合併前找到，第六處是合併之後的盲審找到的）。現在 workflow 呼叫的就是這一支，兩種輸入來源只差在讀檔那一層。⚠️ 本節這條指令的輸出與搬家前**逐位元相同**（`tests/dx/fixtures/analyze_probe/archive.golden.txt` 釘住這一點），所以本文其餘各節引用的數字不受搬家影響。

資料進 repo 的理由與隔壁 `bench-aa-2026-08/` 相同：**來源是 job log，而 job log 有 90 天保留期**（本批 2026-12-03 到期），且 artifact 那條路在本專案的分析環境走不通。三份 `probe-run{1,2,3}.txt` 是從 job log 逐字抽出的 `PROBEENV` / `PROBEROW` / `PROBETAIL` 記錄，各 343 行。

## 量到什麼

### 一、寫入路徑的 writeback 尾巴是真的

| dispatch | 中位輪 | 輪間散佈 | `write` 佔全時間 | 最差 `write_p99` | 最慢的單次寫入 |
|---|---|---|---|---|---|
| 33881597190 | 3881.3 ms | 10.67% | 0.942% | **3300.1 µs** | 8933.8 µs |
| 33881616306 | 3632.4 ms | 8.00% | 0.803% | 158.1 µs | 7703.5 µs |
| 33881623584 | 3684.5 ms | 3.76% | 0.829% | 134.4 µs | 6035.9 µs |

一次 ~150 bytes 的 `os.WriteFile` 花掉 6–9 ms，三次 dispatch 都有。第一次 dispatch 的 `write_p99` 是另外兩次的 **20.9×**（3300.1 / 158.1）與 **24.6×**（3300.1 / 134.4）——那不是單一離群值，是一段**密集**的 stall 期。⇒ 機制 1 描述的現象存在，而且這是它第一次被量到而不是被假設。

⛔ **本文初版把上面兩個比值寫成單一的「22 倍」，那是錯的。** 22 不是任何一次實測值——實算是 20.87 與 24.55，而我把它們併成一個看起來像量測結果的數字。由 CodeRabbit 在 PR #1716 指出，我實算重現後確認成立。⚠️ 這正是本文件其他地方反覆強調的那種錯：**散文裡的數字必須對得上一條跑過的指令**。`analyze_probe.py` 現在直接印出這三個 `write_p99`（`write tail` 那一列），比值可以現場除，不必再有人記一個湊出來的倍率。

### 二、量級：差 19 倍

| | |
|---|---|
| 要解釋的（#1497 同日三次 dispatch 的高低模差距，+30.38% vs +3.98%） | **26.40 pp** |
| 換算到 3698 ms 的一輪上 | **976 ms** |
| 90 輪裡 `write_sum` 高於中位最多的一輪 | **50.3 ms** |
| ⇒ | **差 19×** |

換算到每次迭代更直觀：中位輪的寫入平均 **77.1 µs**；要達標得在全部 400 次迭代上平均 **2.44 ms**，即整個寫入路徑成本的 **32 倍**，而且是**持續**不是尖峰。而實測的 stall 是稀疏的：即使在抓到 stall 期的那一次 dispatch，最慢那一輪的超額有 **83.5%** 落在載入側（`+237.7 ms` vs `+47.0 ms`）。

⛔ **這是對已觀測 episode 的上界，不是不可能性的證明。** 要推翻它，需要一次比 90 輪裡任何一輪都大 19 倍的 writeback episode——本輪沒有看到，也沒有枚舉過會產生那種 episode 的條件。

### 三、這批變異是什麼形狀：水位平移，不是 episode

`load_sum` 變大有兩種完全相反的成因，而在總和上長得一模一樣：每一次迭代都變慢（水位平移），或少數幾次卡住（episode）。把 `load_sum` 在該輪**自己的** p50 切開就分得出來。

| 分量 | corr(round total, ·)（pooled 90 輪） | 自身 sd | 自身 sd ÷ round total sd |
|---|---|---|---|
| 水位 `p50 × iters` | **+0.981** | 103.94 ms | 91.3% |
| 水位以上的質量 | +0.338 | 21.89 ms | 19.2% |
| `write_sum` | +0.692 | 6.42 ms | 5.6% |

逐 dispatch 的「水位以上的質量」相關係數是 **−0.255 / +0.124 / −0.076**——正負號不一致，等於零。

具體到最慢的那一輪（run1 round 10，比中位輪慢 +7.34%）：`load_p50` 由 7.056 → **7.812 ms**（+10.7%），而 `load_p90` 18.696 → 18.801、`load_p99` 19.877 → 20.122（+0.6% / +1.2%）。**中位的那一次迭代變慢了，尾巴幾乎沒動。**

> ⚠️ **這一段的六個數字 `analyze_probe.py` 不印**，所以〈重算〉那條指令重現不了它們。
> 它們是從 `probe-run1.txt` 的 `round=10` 與 `round=14` 兩列 `PROBEROW` 直接讀出來、
> 手算百分比得到的（`load_p50=7812118` / `7055919` ns 等）。要核對請直接 grep 那兩列。
> ⚠️ 另外，n=30 是偶數，`statistics.median` 取的是中間兩值的平均，所以**不存在
> 「就是中位」的單一輪**；這裡的「中位輪」指最接近中位數的那一輪（`round=14`）。

⚠️ 三個不能省的界線：

1. **`write_sum` 的相關係數 +0.692 不小。** 但它的整個動態範圍只有 55.4 ms，對上 round total 的 582.3 ms 全距——**它跟著動，但推不動**。相關與量級要分開讀，這也是上表刻意有兩欄的理由。
2. **⛔ 最後一欄不是 variance 分解，三列不會加總到 100%。** 三個分量彼此相關，所以某一列可以超過 100%（在 `p50` 切點下，水位變大時「水位以上的質量」會被同時吃掉一部分，兩者反向）。⚠️ 這是從切法本身推出來的，而現在**也量得到**：§四 的 harness 在一份純水位平移的合成資料上，量到水位那一列超過 100%、且「水位以上的質量」與 round total **反向**（相關係數約 −1）。⛔ 更正：原文寫「不是量出來的」，那句話在 harness 補回來之前為真、現在不再是。
3. **⛔ 上表的 pooled 那一欄是「生態相關」，被跨 dispatch 的均值差異污染。** 三次 dispatch 的中位輪本身就差 6.75%，把 90 輪攤平算相關係數，會把 dispatch 之間的系統性差異也算進去。把每個 dispatch 各自的均值先減掉再 pool，實測是：水位 `+0.9547`、水位以上的質量 **`−0.1271`**（**符號翻轉**）、`write_sum` `+0.5998`。⇒ **支撐「不是 episode」這個結論的，是上面逐 dispatch 那三個值，不是表格裡的 `+0.338`。** 結論方向不受影響（去均值後更遠離 episode），但那是這批資料剛好如此，不是這個算法保證的。

> ⚠️ **這三個去均值後的數字 `analyze_probe.py` 也不印**（它只做 pooled 與逐 dispatch，沒有去均值那一步），所以〈重算〉那條指令同樣重現不了。要核對的做法：沿用 `analyze_probe.py` 裡 `SHAPE OF THE ROUND-TO-ROUND VARIATION` 那一段開頭的四個分量定義——`tot = write_sum + load_sum`、`base = load_p50 × iters`、`above = load_sum − base`、`w = write_sum`（⚠️ 請用變數名找，不要用行號——行號會隨編輯漂移）——**對每個 dispatch 各自減去該 dispatch 的均值後再併起來**算 Pearson。
> ⛔ **這份報告的「可重算」承諾有缺口，缺口的完整清單如下**（找法：把 §一~§三 所有含小數點的數值抽出來，逐一比對是否出現在 `python3 analyze_probe.py` 的輸出裡。⚠️ 刻意不寫「共幾個」——本表自己也在 §三 之內，它列出的數字會被這個掃描數進去，那個計數自我指涉且每次編輯都會變）：
>
> | 位置 | 值 | 性質 |
> |---|---|---|
> | §三 本節 | `+0.9547` / `−0.1271` / `+0.5998` | 腳本不做去均值那一步 ⇒ **要自己算**，作法見上 |
> | §三 上一段 | `load_p50/p90/p99` 六個值 | 腳本不印 ⇒ **要自己 grep**，作法見該段 |
> | §一 | `20.9×` / `24.6×`（及 `20.87` / `24.55`） | 由腳本印出的三個 `write_p99` **現場相除**得到，該段已寫明 |
> | §二 | `83.5%` / `+237.7 ms` / `+47.0 ms` | 腳本印超額 `+284.8 ms`、寫入側 `+46.982 ms`（佔 `16.5%`）。⚠️ `237.7` 是**直接量到的載入側**（`worst.load_sum − median(load_sum)`），不是 `超額 − 寫入側`（那是 237.8）——兩者差 0.1 ms，因為「和的中位數 ≠ 中位數的和」 |
> | §二 | `30.38` / `3.98` | **不是本報告量的**，是 #1497 自己的兩個模值，本報告的輸入 |
> | §三 第 2 點 | 「超過 100%」/「反向」 | 由 **§四 的 `counterfactual.py`** 在合成資料上量得，不是 `analyze_probe.py` 對本目錄資料的輸出 ⇒ 要核對請跑那一支 |

### 四、這個判別式的判讀力：有可重跑的證據了

```bash
python3 -B docs/internal/audit-reports/bench-probe-2026-09/counterfactual.py
```

不連網、不需要 Go、不讀本目錄的資料。它產生兩份**形狀已知**的合成 `PROBEROW`（取分位與 Go 探針同一套：`probeQ(sorted, q) = sorted[int(q*(len-1))]`，不內插），餵給 `scripts/tools/dx/analyze_probe.py` **本體**（`load` → `reject` → `render_ci` / `render_archive`），再從它**印出來的表**把數字刮回來。⛔ 它不自己算相關係數——隔壁 [`bench-trend-2026-08/counterfactual.py`](../bench-trend-2026-08/counterfactual.py) 的 docstring 記著為什麼：重寫一份邏輯，量到的是一個**模型**，不是這段碼。

⛔ **精確數字、通過數、mutant 數只在該指令的輸出裡，本節刻意不抄**——沒有機械化 SSOT 的數字會漂，而那正是舊那組數字消失的原因。

| # | 問的問題 | 結論 |
|---|---|---|
| 1 | 受測物是誰 | 印出 `analyze_probe.py` 的路徑與 sha256；表格三列的標籤是從它的 **AST** 讀出來的，不是手抄 |
| 2 | 純水位平移 | 水位相關係數 ~+1、「水位以上的質量」~−1，且兩者的 sd 佔比**與切法的代數預測相符**（誤差 <2 pp） |
| 3 | 純 episode | 兩個分量的相關係數**都是 ~+1.000**，sd 佔比卻一個只有幾個百分點、一個接近全部 |
| 4 | 兩欄各自夠不夠 | 只讀相關係數分不出這兩種形狀（水位那一列兩邊都是 ~+1）；加上 sd 佔比才分得出 |
| 7 | `total()` 的兩半都在嗎 | 一份變異全落在**寫入側 tail**（少數幾次寫入卡住）的合成資料：`write_sum` 相關係數 ~+1、sd 佔比 ~100%，載入側兩列安靜 |
| 5 | 這些檢查有沒有偵測力 | 一組故意改壞的判別式，逐一確認**每一項可評估的檢查**至少被其中一種打紅；基準線本來就紅、或只被「讓報表讀不出來」的 mutant 打紅的，不算數 |
| 6 | 驗的是哪一份 | 三種形狀上，兩個 renderer 的 corr / sd / 佔比逐一相符、**佔比 == sd ÷ round total sd**，且 round total sd 對得上 harness 自己產生的那些列（絕對值錨點——比值看不見兩邊一致的單位錯誤） |

⇒ **§三 那張表第二欄的存在理由，現在是量出來的**：檢查 3 就是它的直接證據——一個相關係數 ~+1.000 的分量可以只佔幾個百分點的 sd，只讀第一欄會把它讀成因。

⛔ **舊數字沒有被重現，這支 harness 也沒有試。** 前一版本節列的 `+1.000` / `150.8%` / `+0.791` / `1.1%` / `−0.999` 與三組「故意改壞」紀錄，其 harness 從未進 repo、現已不存在，只憑數字回推不出當初的參數。新 harness 的合成參數是**另外選的**，每次執行都印在輸出最前面。⇒ **兩組數字不可比，以新的為準。**

⛔ **這支 harness 每一輪的修法本身也送去盲審，而每一輪都被打穿——包括修法自己帶進來的新洞。** 逐條的 finding、重現指令與各自的修法在 CHANGELOG 本條目裡；本節只留**還會影響讀者怎麼看待這些數字**的兩件事。

⚠️ **一項沒照盲審做**：它要求把「工具的 `corr()` 壞掉」由 rc 3 降成 rc 1。出界的數字**分不出**是刮錯欄位還是工具壞了，選一個就是宣稱看不見的東西。改的是**訊息**，現在兩種成因都講。

⚠️ **兩個已知盲點，寫下來不修——這是判讀這份證據時的界線：**

1. 三種形狀都對 round total **單調** ⇒ `corr()` 換成別的保序統計量（Spearman）整份全過。要補得加非單調形狀；rank correlation 不是合理的迴歸。
2. **`level()` / `above()` 的加法偏移看不見，而且補不了**：這兩個值的原始大小**從來不會被印出來**，只有它們的相關係數與 sd 會，而兩者都平移不變 ⇒ 這一類錯誤在工具的輸出裡對**任何**消費者都不可觀測，不只是對這支 harness。（`total()` 那一半可觀測，因為中位數會印，已經補上。）

⚠️ **界線：驗的是「形狀已知的合成資料」。** 它證明兩欄一起讀分得出這兩種形狀，**不**證明真實資料只有這兩種形狀，也不證明真實資料上這個判別式一定讀得對。§三 對「這批資料是哪一種形狀」的結論仍由 §三第 3 點獨立支撐。

⚠️ **目前沒有任何 CI 或測試在跑它**（隔壁 `bench-trend-2026-08/` 的先例也一樣）。它壞掉時會**大聲**壞：`render_ci` 的表格改名或改列 ⇒ rc=3 並印 `::error::COULD NOT MEASURE`，與「跑了、判別式有問題」的 rc=1 分開；但沒人跑就沒人看到。要不要把它接上 `tests/dx/`，是尚未裁決的問題。

## ⛔ 沒有做的事（別把它讀成已做）

- **沒有重現雙峰。** 三個 dispatch 的中位數散佈 6.75%，票裡是 26.40 pp。這是**這次沒抓到**，不是「沒有」。
- **沒有回答「缺陷 vs 特性」。** 機制 1 被量到且被量級擋下，但載入側那 83.5% 的超額**本輪沒有成因**。
- **沒有排除「IO 壓力」這個更廣的東西。** 本探針量的是 `os.WriteFile` 這個呼叫本身；writeback 壓力原則上也可能拖慢載入側（載入側有 1000 次 `fstat` 與一次 `ReadFile`）。本輪關掉的是「時間花在寫入呼叫上」，**不是**「IO 與這件事無關」。
- **沒有量成對比值。** 探針量的是**單側絕對時間**；#1497 的量是 M/W 比值。上文把「單側擺 26.40%」當成產生 26.40 pp 比值擺動的條件，那是在「另一側穩定」的假設下——該假設本輪**沒有驗**。
- **只有一個 host class、一小時、三次 dispatch。** 沒有跨 CPU 型號、沒有跨日。
- **⛔ 〈重算〉那條指令不覆蓋本文全部的數字。** 完整缺口與各自的核對方式**逐組**列在 §三第 3 點下方的表——請直接讀那張表，不要讀本句的概括。
- **判別式在真實工作負載上的判讀力沒有驗證。** §四 補回的 harness 驗的是**形狀已知的合成資料**，不是真實資料。本文只對「這批資料是哪一種形狀」下結論，那個結論由 §三第 3 點獨立支撐。
- **沒有動任何產品碼。** 本輪只加了 workflow summary 的兩段輸出。

## 與其他票的關係

- [#1497](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1497)：驗收條件第 1 項的四個候選裡，機制 2 已由分支計數排除、`b.N` 放大已由 `400x` 釘死排除、**機制 1 本輪由量級排除為產生器**。四條驗收條件仍**逐條未滿足**，本票不該關。
- [#1545](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1545)：§三的水位平移發生在 `WARMUP` 之後的穩態迭代上，與該票 2 秒 mtime 窗造成的暖機階梯是**不同的**兩件事（暖機那 135 迭代在本批資料裡沒有跟著動）。
