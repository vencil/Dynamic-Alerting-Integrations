# 機制 1（寫入路徑 writeback flush）的量測（2026-09-04）

> 主要消費者是 [#1497](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1497) 驗收條件第 1 項（「有一個**量測**回答缺陷 vs 特性」）裡唯一還沒被碰過的候選成因。
> 隔壁 [`bench-aa-2026-08/`](../bench-aa-2026-08/README.md) 是同一條線的前一份（A/A 校準與迭代層級成本）。

## 一句話

**機制 1 存在、抓到了，而且比需要的小一個數量級以上。** 三次同日 dispatch、90 個量測輪，寫入路徑確實有 writeback 尾巴（單次寫入最慢 6.0–8.9 ms，其中一次 dispatch 的 `write_p99` 是另兩次的 **20.9×** 與 **24.6×**）；但要產生 #1497 那個 26.40 pp 的量級，寫入路徑得多出 **976 ms**，而 90 輪裡最大的一次寫入路徑超額是 **50.3 ms**——**差 19 倍**。

⛔ **這不等於回答了 #1497 的「缺陷 vs 特性」，也不等於打死機制 1。** 上面那個 19× 是對**已觀測** episode 的上界；本次三次 dispatch **沒有重現**票裡的雙峰（三個中位數的散佈是 6.75%，票裡的高低模差距是 26.40 pp），所以高模式本身沒有被抓到。

⭐ **順帶量到一件本來不在問題清單上的事**：這 90 輪之間的變異**不是 episode，是水位平移**——慢的那一輪連 p50 都變慢，而尾巴沒動。詳下方「三、這批變異是什麼形狀」。這對機制 1 是**反向**訊號，因為機制 1 預測的正是 episode。

## 受測物識別特徵

⛔ 這條線燒過五次「數字跑在錯的樹上」，所以識別特徵在任何比較之前先印，而且由執行中的程式自己印（`PROBEENV`，來自 `runtime.Version()`），不靠人從 shell 抄。

| | |
|---|---|
| workflow | `.github/workflows/bench-probe-write-latency.yaml`，`rounds=30` `iters=400` |
| runs | [33881597190](https://github.com/vencil/Dynamic-Alerting-Integrations/actions/runs/33881597190) / [33881616306](https://github.com/vencil/Dynamic-Alerting-Integrations/actions/runs/33881616306) / [33881623584](https://github.com/vencil/Dynamic-Alerting-Integrations/actions/runs/33881623584) |
| head | `e42526e57d90a08f0537d7d45637ef18219f96fc`（三次相同） |
| runner | AMD EPYC 7763 64-Core、`nproc=4`（**三次相同**，由 job log 的 `model name` 逐次核對） |
| toolchain | `go1.26.7`，PATH 與模組內同版（`go_on_path` / `go_in_module` 兩行都印了） |
| `PROBEENV` | `goos=linux goarch=amd64 numcpu=4 go=go1.26.7 iters=400`，三次去重後**只有一組** |

⇒ 三次 dispatch 的中位輪相差 6.75%，而它們**同一個 host class**——那個差不是 CPU 型號造成的。

⚠️ **一個沒有對齊的量級，明寫**：探針每迭代 3698/400 = **9.25 ms**。這條線自己的 CI 量測有兩族：#1497 的 M/W 二分是 8.69 → 12.12 ms、[#1572](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1572) 的 benchstat 是 17.44 → 19.83 ms。9.25 ms 落在第一族的量級，但比該族 post-#1521 那一點低 24%（那一點量在 9V74 上、今天量在 7763 上）；與第二族仍差約 2×。**這一項本輪沒有去對齊**，它是交接件 §2 已經記著的未結項，不是本輪新開的。

## 重算

```bash
python3 docs/internal/audit-reports/bench-probe-2026-09/analyze_probe.py
```

不連網、不重跑 benchmark、不需要 Go。它回答的是「本文的數字能不能從收進來的資料重算出來」，**不是**「今天重跑會得到什麼」。

資料進 repo 的理由與隔壁 `bench-aa-2026-08/` 相同：**來源是 job log，而 job log 有 90 天保留期**（本批 2026-12-03 到期），且 artifact 那條路在本專案的分析環境走不通。三份 `probe-run{1,2,3}.txt` 是從 job log 逐字抽出的 `PROBEENV` / `PROBEROW` / `PROBETAIL` 記錄，各 343 行。

## 量到什麼

### 一、寫入路徑的 writeback 尾巴是真的

| dispatch | 中位輪 | 輪間散佈 | `write` 佔全時間 | 最差 `write_p99` | 最慢的單次寫入 |
|---|---|---|---|---|---|
| 33881597190 | 3881.3 ms | 10.67% | 0.942% | **3300.1 µs** | 8933.8 µs |
| 33881616306 | 3632.4 ms | 8.00% | 0.803% | 158.1 µs | 7703.5 µs |
| 33881623584 | 3684.5 ms | 3.76% | 0.829% | 134.4 µs | 6035.9 µs |

一次 ~150 bytes 的 `os.WriteFile` 花掉 6–9 ms，三次 dispatch 都有。第一次 dispatch 的 `write_p99` 是另外兩次的 **20.9×**（3300.1 / 158.1）與 **24.6×**（3300.1 / 134.4）——那不是單一離群值，是一段**密集**的 stall 期。⇒ 機制 1 描述的現象存在，而且這是它第一次被量到而不是被假設。

⛔ **本文初版把上面兩個比值寫成單一的「22 倍」，那是錯的。** 22 不是任何一次實測值——實算是 20.87 與 24.55，而我把它們併成一個看起來像量測結果的數字。由 CodeRabbit 在 PR #1716 指出，我實算重現後確認成立。⚠️ 這正是本文件其他地方反覆強調的那種錯：**散文裡的數字必須對得上一條跑過的指令**。`analyze_probe.py` 現在直接印出這三個 `write_p99`（`write tail` 那一列），比值可以現場除，不必再有人記一個湊出來的倍率。

### 二、量級：差 19 倍

| | |
|---|---|
| 要解釋的（#1497 同日三次 dispatch 的高低模差距，+30.38% vs +3.98%） | **26.40 pp** |
| 換算到 3698 ms 的一輪上 | **976 ms** |
| 90 輪裡 `write_sum` 高於中位最多的一輪 | **50.3 ms** |
| ⇒ | **差 19×** |

換算到每次迭代更直觀：中位輪的寫入平均 **77.1 µs**；要達標得在全部 400 次迭代上平均 **2.44 ms**，即整個寫入路徑成本的 **32 倍**，而且是**持續**不是尖峰。而實測的 stall 是稀疏的：即使在抓到 stall 期的那一次 dispatch，最慢那一輪的超額有 **83.5%** 落在載入側（`+237.7 ms` vs `+47.0 ms`）。

⛔ **這是對已觀測 episode 的上界，不是不可能性的證明。** 要推翻它，需要一次比 90 輪裡任何一輪都大 19 倍的 writeback episode——本輪沒有看到，也沒有枚舉過會產生那種 episode 的條件。

### 三、這批變異是什麼形狀：水位平移，不是 episode

`load_sum` 變大有兩種完全相反的成因，而在總和上長得一模一樣：每一次迭代都變慢（水位平移），或少數幾次卡住（episode）。把 `load_sum` 在該輪**自己的** p50 切開就分得出來。

| 分量 | corr(round total, ·)（pooled 90 輪） | 自身 sd | 自身 sd ÷ round total sd |
|---|---|---|---|
| 水位 `p50 × iters` | **+0.981** | 103.94 ms | 91.3% |
| 水位以上的質量 | +0.338 | 21.89 ms | 19.2% |
| `write_sum` | +0.692 | 6.42 ms | 5.6% |

逐 dispatch 的「水位以上的質量」相關係數是 **−0.255 / +0.124 / −0.076**——正負號不一致，等於零。

具體到最慢的那一輪（run1 round 10，比中位輪慢 +7.34%）：`load_p50` 由 7.056 → **7.812 ms**（+10.7%），而 `load_p90` 18.696 → 18.801、`load_p99` 19.877 → 20.122（+0.6% / +1.2%）。**中位的那一次迭代變慢了，尾巴幾乎沒動。**

⚠️ 兩個不能省的界線：

1. **`write_sum` 的相關係數 +0.692 不小。** 但它的整個動態範圍只有 55.4 ms，對上 round total 的 582.3 ms 全距——**它跟著動，但推不動**。相關與量級要分開讀，這也是上表刻意有兩欄的理由。
2. **⛔ 最後一欄不是 variance 分解，三列不會加總到 100%。** 三個分量彼此相關；水位平移時「水位」與「水位以上的質量」反向（合成資料實測 corr −0.999），所以某一列可以超過 100%。

### 四、這個判別式是怎麼驗的（反事實）

上面那張表的判讀力不是推論出來的，是**用兩份形狀已知的合成資料反事實驗過**的：模擬逐迭代延遲、用與 `probeQ` 相同的取分位方式（`int(q*(n-1))`，不內插）產生 `PROBEROW`，再餵給**從 workflow 逐字抽出**的那段 summarize（不是重打一份）。

| 合成形狀 | 水位那一列 corr | 水位那一列 sd 佔比 | 水位以上那一列 corr | sd 佔比 |
|---|---|---|---|---|
| 純水位平移 | +1.000 | **150.8%** | −0.999 | 50.8% |
| 純 episode（稀疏 stall） | +0.791 | **1.1%** | +1.000 | 99.1% |

⭐ **決定性的一點：相關係數自己分不出來。** 兩種形狀下水位那一列都是正且大（+1.000 / +0.791）。把兩者分開的是 sd 那一欄的 **150.8% vs 1.1%**。⇒ 這張表**必須兩欄一起讀**，這句話有上面這組數字對應，不是設計意圖的複述。

三組故意改壞（每組都確認過破壞後的輸出，還原後皆回到對照組）：

| 改壞 | 破壞後 | 對照組 |
|---|---|---|
| `MIN_ROUNDS_FOR_CORR` 5 → 0，餵 4 輪 | 印出 `+1.000 / −0.996 / +0.991` 一張看起來像結論的表，其中 `write_sum` 那個 +0.991 是 0.08 ms sd 上的假相關 | 印「只有 4 輪，少於 5 輪，**不印相關係數**——這是輪數不夠，不是形狀不明顯」 |
| 切點對調（`lvl` ↔ `abv`） | 水位平移的合成資料被報成 episode 簽名（−0.999 / 50.8% vs +1.000 / 150.8%），episode 的被報成水位平移 | 兩者各自報對 |
| （anchor 打錯時 assert 擋下） | 腳本 `AssertionError` 中止 | ——把對照組誤報成破壞組的那條路被守衛擋住了 |

### 五、⛔ 一個「宣稱比程式碼寬」連犯三次的紀錄

這份報告的兩支腳本各有一段「除以量到的量」的統計。那些除數**每一個都可能是 0**，而沒守衛的除法不是讓報告變差，是**讓報告整個死掉**——一次確實收到資料的執行會一個字都不印，於是「量不到」與「量了沒事」被壓成同一件事（沒有字），而那正是這份工具唯一存在的理由。

修這件事花了三輪，每一輪我都宣稱得比程式碼實際做到的寬：

| 輪 | 我宣稱 | 實際 | 是誰抓到的 |
|---|---|---|---|
| 1 | —（沒想到） | 兩處未守衛 | 外部 review |
| 2 | 「摘要不會再死掉」 | workflow 在**更早一行**（`write_sum spread`）就死了，我加的守衛**走不到** | 盲審（換模型、隔離 worktree） |
| 2 | 「同類別都修了」 | `analyze_probe.py` 另有一處 `100*dw/excess` 未守衛 | 自審 |
| 3 | 「非常數除數都守衛了」 | 漏掉 3 處 `st.stdev(tot)`——**因為我用手寫 regex 列除數名字去枚舉** | 自己的 dogfood（degenerate 輸入仍 rc=1） |

⇒ 最後改用 **AST 枚舉每一個除法**（不管除數長什麼樣），才拿到完整清單。現在兩支腳本的非常數除數各 6–7 個，逐一有交代：`corr()` / `pct()` 內部的 `den` 各自守衛、`biggest` 與 `med_w` 有分支守衛、`iters` 由載入時的 `iters<=0` 拒收擋掉（那是**壞掉的記錄**不是 degenerate 但合法的量測，所以拒收而不是印 n/a），`HERE / name` 是 pathlib 路徑接合不是除法。

⚠️ **仍然不宣稱這些狀態不可達**：本節所有 degenerate 情況都是把輸入 construct 出來才到達的，沒有一個是真實 probe run 產生的。守衛的理由是失效模式，不是發生機率。

⭐ **上面那句全稱句是被獨立驗過的，不是我自己說了算**：一位換模型、跑在隔離 detached worktree、看不到本輪任何實作敘事的盲審 reviewer，自己用 AST 重新枚舉兩支腳本並逐一裁決，裁定它在 `7598843` 上成立（它也獨立認出 `HERE / name` 是 pathlib 路徑接合、不是算術除法）。同一位還找到一條我沒發現的**殘留不一致**：`excess <= 0` 時 workflow 把整個歸因子句省略（有 rationale——歸因只在散佈真的出現時才有意義），而 `analyze_probe.py` 照印並填 `n/a`，那反而暗示存在一個歸因。已對齊到有 rationale 的那一邊。

## ⛔ 沒有做的事（別把它讀成已做）

- **沒有重現雙峰。** 三個 dispatch 的中位數散佈 6.75%，票裡是 26.40 pp。這是**這次沒抓到**，不是「沒有」。
- **沒有回答「缺陷 vs 特性」。** 機制 1 被量到且被量級擋下，但載入側那 83.5% 的超額**本輪沒有成因**。
- **沒有排除「IO 壓力」這個更廣的東西。** 本探針量的是 `os.WriteFile` 這個呼叫本身；writeback 壓力原則上也可能拖慢載入側（載入側有 1000 次 `fstat` 與一次 `ReadFile`）。本輪關掉的是「時間花在寫入呼叫上」，**不是**「IO 與這件事無關」。
- **沒有量成對比值。** 探針量的是**單側絕對時間**；#1497 的量是 M/W 比值。上文把「單側擺 26.40%」當成產生 26.40 pp 比值擺動的條件，那是在「另一側穩定」的假設下——該假設本輪**沒有驗**。
- **只有一個 host class、一小時、三次 dispatch。** 沒有跨 CPU 型號、沒有跨日。
- **沒有動任何產品碼。** 本輪只加了 workflow summary 的兩段輸出。

## 與其他票的關係

- [#1497](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1497)：驗收條件第 1 項的四個候選裡，機制 2 已由分支計數排除、`b.N` 放大已由 `400x` 釘死排除、**機制 1 本輪由量級排除為產生器**。四條驗收條件仍**逐條未滿足**，本票不該關。
- [#1545](https://github.com/vencil/Dynamic-Alerting-Integrations/issues/1545)：§三的水位平移發生在 `WARMUP` 之後的穩態迭代上，與該票 2 秒 mtime 窗造成的暖機階梯是**不同的**兩件事（暖機那 135 迭代在本批資料裡沒有跟著動）。
