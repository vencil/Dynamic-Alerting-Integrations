# pytest and coverage configuration has been unified into pyproject.toml
# (see pyproject.toml [tool.pytest.ini_options] and [tool.coverage.*])
#
# This file is intentionally kept empty to avoid dual-config confusion.
# If future tools require setup.cfg sections, add them here with a comment
# explaining why pyproject.toml was not suitable.
